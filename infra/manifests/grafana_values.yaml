adminUser: admin
adminPassword: admin12345

service:
  type: ClusterIP
  port: 80

persistence:
  enabled: true
  size: 2Gi

resources:
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 256Mi

datasources:
  datasources.yaml:
    apiVersion: 1
    datasources:
      - name: vibecheck-prometheus
        type: prometheus
        access: proxy
        url: http://prometheus-server.vibecheck.svc.cluster.local:9090
        isDefault: true
        editable: false
