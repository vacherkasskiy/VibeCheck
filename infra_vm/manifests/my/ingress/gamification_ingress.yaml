apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gamification-service
  namespace: ${NAMESPACE}
spec:
  ingressClassName: ${INGRESS_CLASS_NAME}
  rules:
    - host: gamification.${BASE_DOMAIN}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: gamification-service
                port:
                  number: 8080
